namespace XML_handler
{
    public class Parameters
    {
        public string tagName;
        public string xPath;

        public Parameters(string name, string path)
        {
            tagName = name;
            xPath = path;
        }
    }
}
